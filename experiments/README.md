# Experiments

Phase-specific experiment runners belong here.

Each run must consume an explicit config, write a manifest, and avoid hidden defaults that materially change the experiment. Generated raw artifacts should stay outside Git unless they are small, licensed, and intentionally versioned.
